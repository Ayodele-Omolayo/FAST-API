name: Inventory Service

on:
  push:
    paths: ['inventory-service/**']
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build and push
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/inventory-service:${{ github.sha }} -t ${{ secrets.DOCKER_USERNAME }}/inventory-service:latest -f Dockerfile ./inventory-service
        docker push ${{ secrets.DOCKER_USERNAME }}/inventory-service:${{ github.sha }}
        docker push ${{ secrets.DOCKER_USERNAME }}/inventory-service:latest